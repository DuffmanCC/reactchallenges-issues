name: Auto assign issue

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  assign:
    runs-on: ubuntu-24.04
    steps:
      - run: gh issue edit ${{ github.event.issue.number }} --add-assignee DuffmanCC -R ${{ github.repository }}
        env:
          GH_TOKEN: ${{ github.token }}
      - run: gh issue comment ${{ github.event.issue.number }} --body "Thanks for the issue! I'll try to take a look as soon as possible." -R ${{ github.repository }}
        env:
          GH_TOKEN: ${{ github.token }}
