name: Auto assign issue

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - run: gh issue edit ${{ github.event.issue.number }} --add-assignee DuffmanCC
        env:
          GH_TOKEN: ${{ github.token }}
